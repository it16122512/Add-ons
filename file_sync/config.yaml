name: File Sync SSL
slug: file_sync
version: "2.2.3"
description: "Sync SSL certificates from Nginx Proxy Manager to Asterisk (oneshot job)"
url: https://github.com/it16122512/Add-ons/file_sync
arch:
  - aarch64
  - amd64
  - armv7
build_from:
  aarch64: "ghcr.io/home-assistant/aarch64-base:3.2.7"
  amd64: "ghcr.io/home-assistant/amd64-base:3.2.7"
  armv7: "ghcr.io/home-assistant/armv7-base:3.2.7"
startup: once      # ← Запускается только вручную или через API
boot: manual       # ← Не запускается при старте системы
init: false
hassio_api: true
host_network: false

# Доступ к данным аддонов и SSL
map:
  - ssl:rw
  - addon_configs:ro

options:
  source_relative_path: "a0d7b954_nginxproxymanager/letsencrypt/live/npm-8"
  dest_relative_path: "nginxproxymanager/live/npm-1"
  log_level: info
  timezone: "Europe/Moscow"

schema:
  source_relative_path: str
  dest_relative_path: str
  log_level: list(info|debug|warning|error)
  timezone: str
